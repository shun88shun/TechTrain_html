#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@techtrain/cli-railway/bin/techtrain-railway" "$@"
  ret=$?
else 
  node  "$basedir/../@techtrain/cli-railway/bin/techtrain-railway" "$@"
  ret=$?
fi
exit $ret
